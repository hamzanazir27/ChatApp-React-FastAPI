services:
  - type: web
    name: chatapp
    env: python
    buildCommand: |
      pip install -r server/requirements.txt
      cd frontend && npm install && npm run build
    startCommand: ./server/start.sh
